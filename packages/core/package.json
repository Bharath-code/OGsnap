{
  "name": "@ogsnap/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "echo 'lint:core not configured'",
    "test": "echo 'test:core not configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
