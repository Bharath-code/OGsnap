Renderer tests are intentionally deferred in this scaffold.

Recommended first tests:
- health endpoint returns 200
- /render returns imageUrl for valid HTML
- rejects unauthorized requests when token is configured
