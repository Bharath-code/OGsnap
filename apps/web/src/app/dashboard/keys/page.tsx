export default function DashboardKeysPage() {
  return (
    <section className="card">
      <h2 style={{ marginTop: 0 }}>API Keys</h2>
      <p>Use Convex mutation `apiKeys.mutations.create` to provision keys.</p>
    </section>
  );
}
